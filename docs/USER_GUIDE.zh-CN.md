# DC SaaS 加密货币交易系统用户手册

本文适用于 `saas-crypto` 独立系统。当前生产验证环境为：

- Web：`http://18.140.45.126:18088/`
- 业务数据：MySQL
- 行情与 K 线：ClickHouse
- 租户标识：`location`

> 当前版本用于功能验证和后续产品化开发，不应接入真实资金。页面“充值”是测试环境内部记账，不是链上充值。

## 当前验收状态（核心：2026-08-29；Web：2026-08-30）

已在 `18.140.45.126` 使用两个独立浏览器会话完成 `POSTFIX2_E2E` 闭环：用户名密码登录、测试入金、限价/市价下单、撤单、买卖双方撮合、最近成交、订单/成交历史、资金、持仓和 Market/IOC/ReduceOnly 平仓。自动化验收还覆盖 GTC/IOC/FOK/Post Only、品种精度和最小金额、幂等、批量撤单、资金费、TP/SL/OCO、部分/最终强平、保险基金和事务型 ADL。

最终验收执行 154 个源码测试；生产门禁包含 1000×16 全量回归和 3000×32 高压，峰值轮次 9,000 个订单请求、6,000 条执行、6,000 条资金流水全部精确核对，并通过重启恢复和平仓归零。该结论只代表当前核心交易链路，不代表系统已满足真实资金、安全合规、高可用或交易所级容量要求。

2026-08-30 的 Web 版本已完成公开镜像发布和生产运行验收，包含专业深色双语登录页、可拖动/缩放工作区、最新价行情条、可搜索交易对、下单输入校验和 WebSocket 订阅释放；桌面英文/中文和 390×844 手机英文/中文均由远端浏览器自动化验证。

同日已完成 SaaS 控制面双租户生产验收：公开申请、平台审批、独立 URL、租户内注册、租户管理员、同名用户隔离、品种规则、交易记录查询、租户暂停/恢复和审计均通过真实 GW、服务与 MySQL 链路验证。浏览器验收同时覆盖申请页、注册页、平台运营页和租户管理页的桌面及 390×844 手机布局。

## 1. 登录

1. 从运营人员处取得用户名、密码和所属 `location`。
2. 打开租户登录地址：

   ```text
   http://18.140.45.126:18088/#/login?location=<你的 location>
   ```
3. 输入用户名和密码后单击“登录”。
4. 登录成功后进入交易页。租户专用地址格式为：

   ```text
   http://18.140.45.126:18088/#/trade?location=<你的 location>
   ```

不要修改或省略运营人员提供的 `location`。订单、成交、持仓、余额、资金流水、行情订阅和服务端查询都以它作为租户边界。

审批通过且租户开启“允许注册”后，用户可以从该租户登录页进入自助注册。当前尚未开放找回密码、二次验证、邮箱验证或 KYC；租户管理员也可以直接创建用户和重置初始密码。

### 1.1 申请 SaaS 试用

1. 打开 `http://18.140.45.126:18088/#/apply`。
2. 填写企业/团队名称、租户代码、联系人、邮箱、预计用户数、试用天数、交易品种和使用场景。
3. 提交后保存申请编号；使用申请编号和联系邮箱可查询审批进度。
4. 平台运营批准后，查询结果会显示独立 `location` 和租户访问 URL；初始租户管理员账号由运营人员安全交付。

同一申请请求凭证重复提交不会重复建租户。租户代码和 `location` 只能使用大写字母、数字与下划线，并以字母开头。

### 1.2 租户内注册

1. 从租户登录页单击“注册交易账户”，或打开：

   ```text
   http://18.140.45.126:18088/#/register?location=<你的 location>
   ```
2. 确认页面中的 `location` 与租户 URL 一致。
3. 填写用户名、显示名、邮箱和至少 8 位密码。
4. 注册成功后返回租户登录页。系统同时创建 USDT 账户、用户交易配置和已启用品种配置。

用户名只要求在当前租户内唯一；不同租户可以使用相同用户名，但用户 ID、密码、余额、订单、持仓和权限完全独立。

### 1.3 租户管理员

租户管理员登录后打开顶部 `Tenant Admin / 租户管理`，可以：

- 创建、启用或停用本租户用户，并重置密码；不能停用自己当前使用的管理员账户。
- 配置本租户交易品种、精度、数量、最小名义金额、费率、资金费周期和风险档位。
- 创建与撤销本租户 API Key；Secret 只在创建成功时显示一次。
- 配置 RobotSvr 流动性策略的报价档位、价差、库存上限、刷新周期、熔断与对冲参数。
- 按用户、品种查询订单、成交、持仓、强平和 ADL 流水。
- 管理注册/交易开关、默认语言和品牌配置，并查看不可跨租户的审计日志。

管理员接口以登录会话中的 `location` 为准。即使客户端请求填写另一个 `location`，服务端也会拒绝，不会执行跨租户查询或修改。

### 1.4 平台运营

平台运营使用独立 `PLATFORM` 管理员登录 `/#/platform-login`，审核试用申请并完成租户初始化。运营后台可查看租户状态、试用到期时间和 URL，执行 `TRIAL / ACTIVE / SUSPENDED / EXPIRED` 生命周期管理。暂停或过期后，新注册、交易和普通用户登录会被服务端阻止；恢复为 ACTIVE 后重新开放。

## 2. 交易页布局

- 顶部：当前交易对、最新成交价、24 小时行情、标记价格、指数价格、资金费率及布局工具。
- 默认工作区：K 线图、订单簿/最近成交、下单、持仓/委托/成交、账户资产五个面板。
- 桌面宽屏默认采用专业永续合约布局；窄屏会按断点自动重排，字段和交易能力保持不变。

默认交易对为 `BTCUSDT`。点击左上角交易对可打开 Markets 抽屉；可使用中文/英文搜索框按交易对代码过滤，空结果会明确提示，选择交易对后抽屉自动关闭。

### 2.1 自定义工作区

1. 页面处于“锁定布局”按钮可见的编辑状态时，每个面板右上角会显示拖动手柄。
2. 按住手柄可拖动面板；按住面板右下角可改变宽度和高度，其他面板会自动避让和重排。
3. 单击“锁定布局”可关闭拖动和缩放，防止交易时误操作。
4. 单击“恢复默认”可清除当前自定义布局。

布局按 `location + 用户` 保存在当前浏览器，刷新页面后继续生效；不同租户、用户和浏览器互不共享。如果清理浏览器站点数据，布局会恢复默认。

### 2.2 中英文切换

页面右上角提供“中文 / EN”切换。选择会保存在当前浏览器中，登录、行情、下单、订单、成交、持仓、资金和状态字段会随语言切换；`BTCUSDT`、`USDT`、`GTC/IOC/FOK/PostOnly` 等交易协议标识不翻译。

## 3. 添加测试资金

1. 在 `Account Info` 区域单击 `Deposit`。
2. 输入 USDT 数量。
3. 单击 `Confirm Deposit`。
4. 检查 `Total` 和 `Available` 是否增加。
5. 打开底部 `Funds` 页签，确认出现类型为 Deposit 的资金流水。

余额字段含义：

- `Total`：账户权益；当前余额加未实现盈亏。
- `Available`：可下单或提现的可用余额。
- `Position Margin`：持仓占用保证金。
- `Order Margin`：活动委托冻结的保证金和手续费。
- `Frozen`：其他冻结资金。
- `Unrealised PNL`：持仓未实现盈亏。

## 4. 下单

下单前确认交易对、方向、价格、数量和 `location` 均正确。

数量、限价和触发价必须为大于 0 的普通十进制数；输入为空、为 0 或为负数时，Web 会在请求发送前提示并阻止下单。交易精度、最小数量和最小名义金额仍以后端品种规则校验为最终依据。

### 4.1 限价单

1. 在 `Place Order` 中选择 `Limit`。
2. 输入 `Limit Price`。
3. 输入 `Amount`。
4. 做多单击 `Buy/Long`，做空单击 `Sell/Short`。
5. 未立即成交的订单会显示在 `Open Orders`。

系统按价格优先、同价时间优先进行撮合。买价达到或高于卖价时可以成交，实际成交价由撮合簿中的挂单决定。

### 4.2 市价单

1. 选择 `Market`。
2. 输入 `Amount`。
3. 选择 `Buy/Long` 或 `Sell/Short`。

市价单依赖当前租户、当前交易对的有效订单簿。系统不会在空盘口上报错成交；没有可成交盘口时，订单按 IOC 语义撤销未成交数量。市价单还受交易对的最大扫单偏离比例保护：当前 `BTCUSDT` 为 `5%`，买单不会成交到高于最优卖价 `5%` 的档位，卖单不会成交到低于最优买价 `5%` 的档位，超出保护边界的剩余数量自动撤销。

### 4.3 有效期和下单过滤器

- `GTC`：未立即成交的剩余数量继续挂单，直到成交或撤单。
- `IOC`：立即成交可成交数量，剩余数量自动撤销。
- `FOK`：必须立即全部成交，否则整单撤销且不产生部分成交。
- `Post Only`：只允许成为 Maker；如果下单时会立即成交，则整单拒绝。

每笔订单都会校验价格档位、数量档位、最小/最大数量、最小名义金额和交易对最大价格。Web 限价单可选择 `GTC`、`IOC`、`FOK` 或 `PostOnly`；市价单固定使用受价格保护的 `IOC`。

### 4.4 只减仓和平仓

持仓区域的平仓操作会提交 `reduceOnly` 市价单。只减仓订单只允许减少已有同方向仓位，不能反向开仓；当提交数量大于可平数量时，系统按可平数量自动缩量。重复使用同一客户端订单号提交完全相同的请求会返回原订单，参数不同则拒绝，避免网络重试造成重复下单。

### 4.5 条件限价单

1. 选择 `Limit Conditional`。
2. 输入触发价和数量。
3. 选择 `Last Price` 或 `Mark Price` 作为触发源。
4. 选择买入或卖出。

交易核心已具备 TP/SL 与 OCO 的基础触发和互斥语义；当前 Web 条件单入口仍需继续补齐追踪止损、Close on Trigger、回调率和完整保护单配置。

## 5. 撤单

1. 打开底部 `Open Orders`。
2. 找到目标交易对和订单。
3. 单击该行右侧 `Cancel`。
4. 撤单成功后，该订单从活动委托中消失，并在 `Order History` 中显示最终状态。

如果订单在撤单请求到达前已经完全成交，撤单可能被拒绝；应以成交回报和最终订单状态为准。

## 6. 查看成交和最近成交

- `Trade History`：仅显示当前用户的成交回报，包括数量、成交价、手续费、实现盈亏、订单号和成交号。
- `Order History`：显示订单完整生命周期，包括 `New`、`Partially_Filled`、`Filled`、`Canceled` 或拒单状态。
- `Recent Trades`：显示当前 `location`、当前交易对的市场最近成交，不是单个用户的私有成交记录。
- `Positions`：显示当前持仓、开仓均价、标记价格、保证金、强平价和盈亏。

常见状态：

- `Newing`：订单正在受理。
- `New`：订单已进入活动委托。
- `Partially_Filled`：部分成交，仍有剩余数量。
- `Filled`：全部成交。
- `Pending_Cancel`：撤单处理中。
- `Canceled`：已撤销。
- `Rejected`：订单校验或风险检查未通过；查看拒绝原因。

## 7. 当前限制

- 仅适合测试资金，尚未接入托管钱包、链上确认、提现审核和冷热钱包。
- 当前生产验证环境已能接收 Binance Futures ticker/bookTicker，并在 Web 显示 Last/Mark/Index；真实资金上线前仍需多源指数、异常剔除和断源降级。
- 隔离测试允许使用受控参考价；真实生产必须关闭任何“缺少标记价时回退到订单价”的测试开关。
- 已具备精度/数量/名义金额过滤器、`GTC/IOC/FOK/Post Only`、客户端订单号幂等、`reduceOnly`、市价扫单边界、STP、改单和批量撤单；仍需产品化断线全撤、完整双向仓位、全仓/逐仓保证金和更完整风险限额。
- 当前为单机 Docker 部署，没有达到真实资金要求的多可用区、高可用和灾备等级。
- 页面已支持中英文切换；少数后端协议枚举和品种/币种代码保持原始英文标识。

## 8. 使用安全

- 不要共享密码、会话令牌或未来创建的 API 密钥。
- 不要在公共电脑选择记住密码。
- 确认网址、`location` 和交易对后再操作。
- 遇到余额、订单或成交不一致时停止继续下单，记录订单号、成交号、时间和 `location`，由运营人员按 MySQL 订单/账本和服务日志核对。
