CREATE TABLE IF NOT EXISTS dc.quant_order
(
    `id` String,
    `tradeDate` Date32,
    `eventTime` String,
    `createTime` String,
    `updateTime` UInt64,
    `source` String,
    `quantID` String,
    `userID` String,
    `userName` String,
    `algoName` String,
    `strategyName` String,
    `strategyVersion` String,
    `scene` String,
    `strategyPayload` String,
    `venueTypeGW` String,
    `venues` String,
    `location` String,
    `marketIndicator` String,
    `securityID` String,
    `symbol` String,
    `clOrdID` String,
    `orderID` String,
    `execID` String,
    `ordStatus` String,
    `ordStatus2` String,
    `execType` String,
    `side` String,
    `ordType` String,
    `timeInForce` String,
    `positionSide` String,
    `settlType` String,
    `rejectText` String,
    `ordRejReason` String,
    `triggerType` String,
    `triggerPrice` String,
    `orderQty` Nullable(Decimal(38, 12)),
    `leavesQty` Nullable(Decimal(38, 12)),
    `cumQty` Nullable(Decimal(38, 12)),
    `price` Nullable(Decimal(38, 12)),
    `avgPx` Nullable(Decimal(38, 12)),
    `lastPx` Nullable(Decimal(38, 12)),
    `lastQty` Nullable(Decimal(38, 12)),
    `fee` Nullable(Decimal(38, 12)),
    `realizedPnl` Nullable(Decimal(38, 12)),
    `info1` String,
    `info2` String,
    `info3` String,
    `info4` String,
    `info5` String,
    `info6` String,
    `info7` String,
    `info8` String,
    `info9` String
)
ENGINE = ReplacingMergeTree(updateTime)
PARTITION BY toYYYYMM(tradeDate)
ORDER BY (quantID, venueTypeGW, securityID, orderID)
SETTINGS index_granularity = 8192;
